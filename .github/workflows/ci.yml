name: ci

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  repository:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - run: uv sync --locked --dev
      - run: uv run ruff check scripts tests firmware/tools
      - run: uv run pytest
      - run: uv run python scripts/verify_repository.py
      - run: uv run python scripts/check_secrets.py

  server:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - run: uv sync --directory server --locked --all-packages --dev
      - run: uv run --directory server ruff check .
      - run: uv run --directory server pytest

  desktop-reference:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - run: uv sync --directory clients/desktop_reference --locked --extra test
      - run: uv run --directory clients/desktop_reference ruff check src tests
      - run: uv run --directory clients/desktop_reference pytest -m "not e2e_host"

  desktop-reference-host-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - run: uv sync --directory server --locked --all-packages --dev
      - run: uv sync --directory clients/desktop_reference --locked --extra test
      - name: Run deterministic Director/Worker WSS and UDP E2E
        run: >-
          uv run --directory clients/desktop_reference pytest
          tests/e2e/test_deterministic_host.py -m e2e_host

  redis-integration:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7.4.5-alpine3.21@sha256:bb186d083732f669da90be8b0f975a37812b15e913465bb14d845db72a4e3e08
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 2s
          --health-timeout 2s
          --health-retries 15
    env:
      VOICE_TEST_REDIS_URL: redis://127.0.0.1:6379/0
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - run: uv sync --directory server --locked --all-packages --dev
      - name: Run Redis coordination store integration tests
        run: >-
          uv run --directory server pytest
          apps/session_director/tests/test_redis_store.py
          --junitxml=redis-results.xml
      - name: Run Redis Director/Worker subprocess E2E
        run: >-
          uv run --directory server pytest
          tests/e2e/test_horizontal_scale_subprocesses.py
          -m e2e_host --junitxml=redis-horizontal-results.xml
      - name: Reject skipped Redis integration tests
        run: >-
          uv run --directory server python -c
          "import xml.etree.ElementTree as ET;
          paths=['redis-results.xml','redis-horizontal-results.xml'];
          skipped=sum(len(ET.parse(path).getroot().findall('.//skipped')) for path in paths);
          assert skipped == 0, f'{skipped} Redis test(s) skipped'"

  native-firmware-host-contracts:
    runs-on: ubuntu-latest
    env:
      IDF_PATH: ${{ github.workspace }}/.ci/esp-idf
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - name: Check out pinned ESP-IDF
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: espressif/esp-idf
          ref: 30aaf64524299d3bde422ca9a2848090d1bc5d0f
          fetch-depth: 0
          fetch-tags: true
          path: .ci/esp-idf
          submodules: recursive
      - name: Verify native firmware composition
        run: test -f firmware/apps/voice_terminal/main/app_main.cc
      - name: Install native host contract dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libcjson-dev ninja-build
      - name: Build and run native host contracts
        shell: bash
        run: |
          set -euo pipefail
          out="$(mktemp -d)"
          trap 'rm -rf "$out"' EXIT
          g++ -std=c++17 -Wall -Wextra -Werror -O2 \
            -I firmware/components/device_config/include \
            firmware/components/device_config/device_config.cc \
            firmware/components/device_config/host_tests/device_config_test.cc \
            -o "$out/device_config_test"
          "$out/device_config_test"
          g++ -std=c++17 -Wall -Wextra -Werror -O2 \
            -I firmware/components/audio_pipeline/include \
            firmware/components/audio_pipeline/audio_pipeline.cc \
            firmware/components/audio_pipeline/host_tests/audio_pipeline_test.cc \
            -o "$out/audio_pipeline_test"
          "$out/audio_pipeline_test"
          g++ -std=c++17 -Wall -Wextra -Werror -O2 \
            -I firmware/components/ui_lvgl/include \
            firmware/components/ui_lvgl/ui_reducer.cc \
            firmware/components/ui_lvgl/host_tests/ui_reducer_test.cc \
            -o "$out/ui_reducer_test"
          "$out/ui_reducer_test"
          profile_test="firmware/apps/voice_terminal/host_tests/default_media_profile_test.cc"
          profile_include="firmware/apps/voice_terminal/main"
          g++ -std=c++17 -Wall -Wextra -Werror -O2 \
            -I "$profile_include" "$profile_test" -o "$out/default_profile_unset"
          "$out/default_profile_unset"
          g++ -std=c++17 -Wall -Wextra -Werror -O2 \
            -DCONFIG_RVA_DEFAULT_MEDIA_PROFILE_WSS=1 \
            -I "$profile_include" "$profile_test" -o "$out/default_profile_wss"
          "$out/default_profile_wss"
          g++ -std=c++17 -Wall -Wextra -Werror -O2 \
            -DCONFIG_RVA_DEFAULT_MEDIA_PROFILE_UDP=1 -DRVA_EXPECT_UDP=1 \
            -I "$profile_include" "$profile_test" -o "$out/default_profile_udp"
          "$out/default_profile_udp"
          g++ -std=c++17 -Wall -Wextra -Werror -O2 \
            -DCONFIG_RVA_DEFAULT_MEDIA_PROFILE_WSS=1 \
            -DCONFIG_RVA_DEFAULT_MEDIA_PROFILE_UDP=1 \
            -I "$profile_include" "$profile_test" -o "$out/default_profile_conflict"
          "$out/default_profile_conflict"
          g++ -std=c++17 -fno-exceptions -Wall -Wextra -Werror -O2 -pthread \
            -I firmware/components/voice_protocol/include \
            -I firmware/components/transport_wss/include \
            -I /usr/include/cjson \
            firmware/components/voice_protocol/control.cc \
            firmware/components/voice_protocol/media_header.cc \
            firmware/components/transport_wss/wss_owner.cc \
            firmware/components/transport_wss/wss_session.cc \
            firmware/components/transport_wss/host_tests/wss_contract_test.cc \
            -lcjson -o "$out/wss_contract_test"
          "$out/wss_contract_test"
      - name: Run UDP host contracts and GCM fixtures
        shell: pwsh
        run: ./firmware/components/transport_udp/host_tests/run.ps1
      - name: Run native runtime lifecycle contracts
        shell: pwsh
        run: ./firmware/components/native_runtime/host_tests/run.ps1

  native-firmware-build-size:
    runs-on: ubuntu-latest
    container:
      image: espressif/idf:v5.5.2@sha256:05cbfc42ed2e987b8026722c15bf1d8523d3e4fd1b4ac04d2e4056f5e0918b99
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Build native ESP32-S3 firmware with pinned ESP-IDF
        working-directory: firmware/apps/voice_terminal
        shell: bash
        run: |
          set -euo pipefail
          test "$(git -c safe.directory="$IDF_PATH" -C "$IDF_PATH" rev-parse HEAD)" = "30aaf64524299d3bde422ca9a2848090d1bc5d0f"
          /opt/esp/entrypoint.sh idf.py -B build-ci build
      - name: Enforce image size and partition fit
        working-directory: firmware/apps/voice_terminal
        shell: bash
        run: |
          set -euo pipefail
          /opt/esp/entrypoint.sh idf.py -B build-ci size
          test -s build-ci/rva_voice_terminal.bin
      # Build/size proves toolchain reproducibility only; target-board HIL remains a release gate.

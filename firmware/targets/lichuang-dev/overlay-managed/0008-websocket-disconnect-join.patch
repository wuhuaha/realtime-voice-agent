diff --git a/managed_components/78__esp-ml307/include/tcp.h b/managed_components/78__esp-ml307/include/tcp.h
index d4210e6..4cc809b 100644
--- a/managed_components/78__esp-ml307/include/tcp.h
+++ b/managed_components/78__esp-ml307/include/tcp.h
@@ -9,6 +9,7 @@ class Tcp {
 public:
     virtual ~Tcp() = default;
     virtual void SetTimeout(int timeout_ms) { (void)timeout_ms; }
+    virtual bool IsReceiveTask() const { return false; }
     virtual bool Connect(const std::string& host, int port) = 0;
     virtual void Disconnect() = 0;
     virtual int Send(const std::string& data) = 0;
diff --git a/managed_components/78__esp-ml307/include/web_socket.h b/managed_components/78__esp-ml307/include/web_socket.h
index 382964a..7d303ae 100644
--- a/managed_components/78__esp-ml307/include/web_socket.h
+++ b/managed_components/78__esp-ml307/include/web_socket.h
@@ -27,6 +27,8 @@ public:
     bool Send(const void* data, size_t len, bool binary = false, bool fin = true);
     void Ping();
     void Close();
+    bool Disconnect();
+    bool IsReceiveTask() const;
 
     void OnConnected(std::function<void()> callback);
     void OnDisconnected(std::function<void()> callback);
diff --git a/managed_components/78__esp-ml307/src/esp/esp_ssl.cc b/managed_components/78__esp-ml307/src/esp/esp_ssl.cc
index da9f928..757a53f 100644
--- a/managed_components/78__esp-ml307/src/esp/esp_ssl.cc
+++ b/managed_components/78__esp-ml307/src/esp/esp_ssl.cc
@@ -32,6 +32,11 @@ void EspSsl::SetTimeout(int timeout_ms) {
     timeout_ms_ = std::max(1, timeout_ms);
 }
 
+bool EspSsl::IsReceiveTask() const {
+    return receive_task_handle_ != nullptr &&
+           xTaskGetCurrentTaskHandle() == receive_task_handle_;
+}
+
 bool EspSsl::Connect(const std::string& host, int port) {
     if (tls_client_ != nullptr || receive_task_handle_ != nullptr) {
         Disconnect();
diff --git a/managed_components/78__esp-ml307/src/esp/esp_ssl.h b/managed_components/78__esp-ml307/src/esp/esp_ssl.h
index ba17013..a92ab96 100644
--- a/managed_components/78__esp-ml307/src/esp/esp_ssl.h
+++ b/managed_components/78__esp-ml307/src/esp/esp_ssl.h
@@ -16,6 +16,7 @@ public:
     ~EspSsl();
 
     void SetTimeout(int timeout_ms) override;
+    bool IsReceiveTask() const override;
     bool Connect(const std::string& host, int port) override;
     void Disconnect() override;
     int Send(const std::string& data) override;
diff --git a/managed_components/78__esp-ml307/src/esp/esp_tcp.cc b/managed_components/78__esp-ml307/src/esp/esp_tcp.cc
index 96ba962..a64ba17 100644
--- a/managed_components/78__esp-ml307/src/esp/esp_tcp.cc
+++ b/managed_components/78__esp-ml307/src/esp/esp_tcp.cc
@@ -35,6 +35,11 @@ void EspTcp::SetTimeout(int timeout_ms) {
     timeout_ms_ = std::max(1, timeout_ms);
 }
 
+bool EspTcp::IsReceiveTask() const {
+    return receive_task_handle_ != nullptr &&
+           xTaskGetCurrentTaskHandle() == receive_task_handle_;
+}
+
 bool EspTcp::Connect(const std::string& host, int port) {
     // 确保先断开已有连接
     if (connected_ || tcp_fd_ != -1 || receive_task_handle_ != nullptr) {
diff --git a/managed_components/78__esp-ml307/src/esp/esp_tcp.h b/managed_components/78__esp-ml307/src/esp/esp_tcp.h
index f05c9a5..cb118f0 100644
--- a/managed_components/78__esp-ml307/src/esp/esp_tcp.h
+++ b/managed_components/78__esp-ml307/src/esp/esp_tcp.h
@@ -15,6 +15,7 @@ public:
     ~EspTcp();
 
     void SetTimeout(int timeout_ms) override;
+    bool IsReceiveTask() const override;
     bool Connect(const std::string& host, int port) override;
     void Disconnect() override;
     int Send(const std::string& data) override;
diff --git a/managed_components/78__esp-ml307/src/web_socket.cc b/managed_components/78__esp-ml307/src/web_socket.cc
index a2b1f21..ea26a03 100644
--- a/managed_components/78__esp-ml307/src/web_socket.cc
+++ b/managed_components/78__esp-ml307/src/web_socket.cc
@@ -46,8 +46,9 @@ WebSocket::WebSocket(NetworkInterface* network, int connect_id) : network_(netwo
 }
 
 WebSocket::~WebSocket() {
-    if (connected_) {
-        tcp_->Disconnect();
+    if (!Disconnect()) {
+        ESP_LOGE(TAG, "WebSocket destroyed from its receive task");
+        abort();
     }
     if (handshake_event_group_) {
         vEventGroupDelete(handshake_event_group_);
@@ -264,6 +265,27 @@ void WebSocket::Close() {
     }
 }
 
+bool WebSocket::Disconnect() {
+    if (tcp_ == nullptr) {
+        connected_ = false;
+        return true;
+    }
+    if (tcp_->IsReceiveTask()) {
+        ESP_LOGE(TAG, "Refusing to join WebSocket transport from receive task");
+        return false;
+    }
+
+    tcp_->Disconnect();
+    tcp_.reset();
+    connected_ = false;
+    handshake_completed_ = false;
+    return true;
+}
+
+bool WebSocket::IsReceiveTask() const {
+    return tcp_ != nullptr && tcp_->IsReceiveTask();
+}
+
 void WebSocket::OnConnected(std::function<void()> callback) {
     on_connected_ = callback;
 }
@@ -449,4 +471,4 @@ bool WebSocket::SendControlFrame(uint8_t opcode, const void* data, size_t len) {
     // 发送帧
     std::lock_guard<std::mutex> lock(send_mutex_);
     return tcp_->Send(frame) >= 0;
-}
\ No newline at end of file
+}

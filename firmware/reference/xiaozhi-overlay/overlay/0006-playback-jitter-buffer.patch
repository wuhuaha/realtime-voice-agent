diff --git a/main/application.cc b/main/application.cc
index 4bccd78..5aea11d 100644
--- a/main/application.cc
+++ b/main/application.cc
@@ -505,7 +505,7 @@ void Application::InitializeProtocol() {
     });
     
     protocol_->OnIncomingAudio([this](std::unique_ptr<AudioStreamPacket> packet) {
-        if (GetDeviceState() == kDeviceStateSpeaking) {
+        if (incoming_tts_audio_.load(std::memory_order_acquire)) {
             audio_service_.PushPacketToDecodeQueue(std::move(packet));
         }
     });
@@ -519,6 +519,8 @@ void Application::InitializeProtocol() {
     });
     
     protocol_->OnAudioChannelClosed([this, &board]() {
+        incoming_tts_audio_.store(false, std::memory_order_release);
+        audio_service_.ResetDecoder();
         board.SetPowerSaveLevel(PowerSaveLevel::LOW_POWER);
         Schedule([this]() {
             auto display = Board::GetInstance().GetDisplay();
@@ -533,11 +535,15 @@ void Application::InitializeProtocol() {
         if (strcmp(type->valuestring, "tts") == 0) {
             auto state = cJSON_GetObjectItem(root, "state");
             if (strcmp(state->valuestring, "start") == 0) {
+                audio_service_.BeginPlayback();
+                incoming_tts_audio_.store(true, std::memory_order_release);
                 Schedule([this]() {
                     aborted_ = false;
                     SetDeviceState(kDeviceStateSpeaking);
                 });
             } else if (strcmp(state->valuestring, "stop") == 0) {
+                incoming_tts_audio_.store(false, std::memory_order_release);
+                audio_service_.EndPlaybackInput();
                 Schedule([this]() {
                     if (GetDeviceState() == kDeviceStateSpeaking) {
                         if (listening_mode_ == kListeningModeManualStop) {
@@ -928,7 +934,6 @@ void Application::HandleStateChangedEvent() {
                 // Only AFE wake word can be detected in speaking mode
                 audio_service_.EnableWakeWordDetection(audio_service_.IsAfeWakeWord());
             }
-            audio_service_.ResetDecoder();
             break;
         case kDeviceStateWifiConfiguring:
             audio_service_.EnableVoiceProcessing(false);
@@ -951,6 +956,8 @@ void Application::Schedule(std::function<void()>&& callback) {
 void Application::AbortSpeaking(AbortReason reason) {
     ESP_LOGI(TAG, "Abort speaking");
     aborted_ = true;
+    incoming_tts_audio_.store(false, std::memory_order_release);
+    audio_service_.ResetDecoder();
     if (protocol_) {
         protocol_->SendAbortSpeaking(reason);
     }
diff --git a/main/application.h b/main/application.h
index cb635d4..cbb4905 100644
--- a/main/application.h
+++ b/main/application.h
@@ -11,6 +11,7 @@
 #include <deque>
 #include <memory>
 #include <functional>
+#include <atomic>
 
 #include "protocol.h"
 #include "ota.h"
@@ -144,6 +145,7 @@ private:
     bool aborted_ = false;
     bool assets_version_checked_ = false;
     bool play_popup_on_listening_ = false;  // Flag to play popup sound after state changes to listening
+    std::atomic<bool> incoming_tts_audio_{false};
     int clock_ticks_ = 0;
     TaskHandle_t activation_task_handle_ = nullptr;
 
diff --git a/main/audio/audio_service.cc b/main/audio/audio_service.cc
index 350ac58..786676a 100644
--- a/main/audio/audio_service.cc
+++ b/main/audio/audio_service.cc
@@ -1,4 +1,5 @@
 #include "audio_service.h"
+#include <algorithm>
 #include <esp_log.h>
 #include <cstring>
 
@@ -163,7 +164,7 @@ void AudioService::Start() {
         AudioService* audio_service = (AudioService*)arg;
         audio_service->OpusCodecTask();
         vTaskDelete(NULL);
-    }, "opus_codec", 2048 * 12, this, 2, &opus_codec_task_handle_);
+    }, "opus_codec", 2048 * 12, this, 5, &opus_codec_task_handle_);
 }
 
 void AudioService::Stop() {
@@ -288,13 +289,36 @@ void AudioService::AudioInputTask() {
 }
 
 void AudioService::AudioOutputTask() {
+    int64_t wait_started_us = 0;
+    uint32_t underrun_count = 0;
+    int64_t max_wait_us = 0;
+    int64_t max_write_us = 0;
     while (true) {
         std::unique_lock<std::mutex> lock(audio_queue_mutex_);
-        audio_queue_cv_.wait(lock, [this]() { return !audio_playback_queue_.empty() || service_stopped_; });
+        wait_started_us = esp_timer_get_time();
+        audio_queue_cv_.wait(lock, [this]() {
+            return service_stopped_ ||
+                (!audio_playback_queue_.empty() &&
+                 (!playback_prebuffering_ ||
+                  audio_playback_queue_.size() >= PLAYBACK_PREBUFFER_TASKS ||
+                  playback_input_finished_));
+        });
         if (service_stopped_) {
             break;
         }
 
+        const int64_t waited_us = esp_timer_get_time() - wait_started_us;
+        if (!playback_prebuffering_ && !playback_input_finished_ &&
+            debug_statistics_.playback_count > 0 && waited_us > 5000) {
+            underrun_count++;
+            max_wait_us = std::max(max_wait_us, waited_us);
+        }
+        if (playback_prebuffering_) {
+            ESP_LOGI(TAG, "Playback prebuffer ready: frames=%u finished=%d",
+                     static_cast<unsigned>(audio_playback_queue_.size()), playback_input_finished_);
+            playback_prebuffering_ = false;
+        }
+
         auto task = std::move(audio_playback_queue_.front());
         audio_playback_queue_.pop_front();
         audio_queue_cv_.notify_all();
@@ -306,11 +330,23 @@ void AudioService::AudioOutputTask() {
             codec_->EnableOutput(true);
         }
 
+        const int64_t write_started_us = esp_timer_get_time();
         codec_->OutputData(task->pcm);
+        max_write_us = std::max(max_write_us, esp_timer_get_time() - write_started_us);
 
         /* Update the last output time */
         last_output_time_ = std::chrono::steady_clock::now();
         debug_statistics_.playback_count++;
+        if (debug_statistics_.playback_count % 50 == 0) {
+            std::lock_guard<std::mutex> stats_lock(audio_queue_mutex_);
+            ESP_LOGI(TAG,
+                     "Playback diagnostics: frames=%u underruns=%u max_wait_ms=%.1f "
+                     "max_write_ms=%.1f decode_queue=%u playback_queue=%u",
+                     debug_statistics_.playback_count, underrun_count,
+                     max_wait_us / 1000.0, max_write_us / 1000.0,
+                     static_cast<unsigned>(audio_decode_queue_.size()),
+                     static_cast<unsigned>(audio_playback_queue_.size()));
+        }
 
 #if CONFIG_USE_SERVER_AEC
         /* Record the timestamp for server AEC */
@@ -665,6 +701,20 @@ void AudioService::WaitForPlaybackQueueEmpty() {
     });
 }
 
+void AudioService::BeginPlayback() {
+    ResetDecoder();
+    std::lock_guard<std::mutex> lock(audio_queue_mutex_);
+    playback_prebuffering_ = true;
+    playback_input_finished_ = false;
+    audio_queue_cv_.notify_all();
+}
+
+void AudioService::EndPlaybackInput() {
+    std::lock_guard<std::mutex> lock(audio_queue_mutex_);
+    playback_input_finished_ = true;
+    audio_queue_cv_.notify_all();
+}
+
 void AudioService::ResetDecoder() {
     std::lock_guard<std::mutex> lock(audio_queue_mutex_);
     std::unique_lock<std::mutex> decoder_lock(decoder_mutex_);
@@ -676,6 +726,8 @@ void AudioService::ResetDecoder() {
     audio_decode_queue_.clear();
     audio_playback_queue_.clear();
     audio_testing_queue_.clear();
+    playback_prebuffering_ = false;
+    playback_input_finished_ = true;
     audio_queue_cv_.notify_all();
 }
 
diff --git a/main/audio/audio_service.h b/main/audio/audio_service.h
index 99db546..949eac0 100644
--- a/main/audio/audio_service.h
+++ b/main/audio/audio_service.h
@@ -38,7 +38,8 @@
 
 #define OPUS_FRAME_DURATION_MS 60
 #define MAX_ENCODE_TASKS_IN_QUEUE 2
-#define MAX_PLAYBACK_TASKS_IN_QUEUE 2
+#define MAX_PLAYBACK_TASKS_IN_QUEUE 4
+#define PLAYBACK_PREBUFFER_TASKS 4
 #define MAX_DECODE_PACKETS_IN_QUEUE (2400 / OPUS_FRAME_DURATION_MS)
 #define MAX_SEND_PACKETS_IN_QUEUE (2400 / OPUS_FRAME_DURATION_MS)
 #define AUDIO_TESTING_MAX_DURATION_MS 10000
@@ -116,6 +117,8 @@ public:
     bool IsVoiceDetected() const { return voice_detected_; }
     bool IsIdle();
     void WaitForPlaybackQueueEmpty();
+    void BeginPlayback();
+    void EndPlaybackInput();
     bool IsWakeWordRunning() const { return xEventGroupGetBits(event_group_) & AS_EVENT_WAKE_WORD_RUNNING; }
     bool IsAudioProcessorRunning() const { return xEventGroupGetBits(event_group_) & AS_EVENT_AUDIO_PROCESSOR_RUNNING; }
     bool IsAfeWakeWord();
@@ -179,6 +182,8 @@ private:
     bool voice_detected_ = false;
     bool service_stopped_ = true;
     bool audio_input_need_warmup_ = false;
+    bool playback_prebuffering_ = false;
+    bool playback_input_finished_ = true;
 
     esp_timer_handle_t audio_power_timer_ = nullptr;
     std::chrono::steady_clock::time_point last_input_time_;
@@ -192,4 +197,4 @@ private:
     void CheckAndUpdateAudioPowerState();
 };
 
-#endif
\ No newline at end of file
+#endif
